# config.js is now a PUBLIC, token-free file (just the Netlify forwarder URL) and
# IS committed. The bearer token lives only in the Netlify function's env var.
# local revert backup of the storm-engine viewer template (not part of the site)
_lastgood/
__pycache__/
*.pyc
node_modules/

# local-only demo render data (real lead targeting) — never publish
public/sample/

# Windows/browser duplicate-download strays ("foo (1).py") — never source
* (1).*
*\ (1).*
